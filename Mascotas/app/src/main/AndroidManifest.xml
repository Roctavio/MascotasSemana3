<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manager.mascotas">

    <application
        android:allowBackup="true"
        android:icon="@drawable/star"
        android:label=""
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Mascotas">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MascotasOrdenadas"></activity>
        <!--<activity android:name=".MascotasOrdenadas"
            android:parentActivityName=".Mascotas"
            >
            - Soporte para versiones 4.0 o menores -
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Mascotas"></meta-data>

        </activity>-->
    </application>

</manifest>